# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# runtime state for the off-chain fETH wallet/market store (lib/offchainWallet.ts) —
# local dev only, not meant to be committed (unlike data/premium-history/*.jsonl,
# which is intentionally committed history from a cron)
/data/offchain-play.json

# local-file fallback for lib/comments.ts's Redis store — same reasoning as above
/data/comments.json

# typescript
*.tsbuildinfo
next-env.d.ts
